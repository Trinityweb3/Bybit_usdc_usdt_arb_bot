BYBIT_API_KEY / BYBIT_API_SECRET

1. Go to bybit.com, sign in to your account, click your avatar in the top-right corner, then open API (or go directly to "https://www.bybit.com/app/user/api-management").
2. Click Create New Key and choose System-generated API Keys (not Connect to Third-Party App).
3. Enable only the permissions the bot actually needs:
   - Contract → Orders: Read-Write
   - Contract → Positions: Read-Write
   - Leave everything else disabled, especially Withdrawal permissions. A trading bot should never have access to withdrawals.

1. BYBIT_API_KEY = your API Key
2. BYBIT_API_SECRE = your API Secret

TELEGRAM_BOT_TOKEN from @BotFather, it looks like "11111111:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
   
TELEGRAM_CHAT_ID follow instructions from @FIND_MY_ID_BOT 

Strategy parameters

These values are not tied to any external service — you choose them yourself.

1. DRY_RUN is a boolian variable. "true" if you wanna get only logs. Orders dont execute. (test mode). Switch to "false" to correct work.
LEVERAGE. I think it's obviously
USDT_SIZE/USDC_SIZE Position size in dollars
ENTRY_SPREAD_PCT - Minimum spread for entering positions
POLL_INTERVAL_SECS - Update frequency
